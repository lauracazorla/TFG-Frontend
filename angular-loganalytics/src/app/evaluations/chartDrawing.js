console.log("hola");
/*
const chartData = [];
const chartLabels = [];
this.evaluations.forEach((evaluation) => {
    if (evaluation.metric.controller == metric.controller) {
        chartData.push(evaluation.value);
        chartLabels.push(evaluation.internalMetric.param);
    }
});

const chart = new Chart(document.getElementById('chart-${metric.controller}'), {
    type: 'bar',
    data: {
        labels: chartLabels,
        datasets: [
            {
                label: 'Metric Values',
                data: chartData,
                backgroundColor: ['red', 'blue', 'green']
            }
        ]
    },
    options: {
        scales: {
            yAxes: [{
                ticks: {
                    beginAtZero: true
                }
            }]
        }
    }
});*/